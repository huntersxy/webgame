# 生产环境变量（Vite 只暴露 VITE_ 前缀的变量）
#
# 象棋引擎已换成随站点分发的神经网络权重（public/xqnn/chess_model.onnx，8.7MB），
# 不再需要外部 OSS/CDN 托管，因此这里不再有 VITE_PIKAFISH_DATA_BASE。
# 若将来要外链权重，请参照 go/model-assets.ts 的模式新增 VITE_XQNN_MODEL_BASE。
