node_modules/
dist/
.tmp/
*.local
.vite/
/.workbuddy

# EdgeOne 部署工具在本地产出的链接文件与暂存副本（含整份构建产物，约 50MB）。
# 以后若用 token 登录，凭据也可能落在这里，务必保持忽略。
.edgeone/

# 环境变量：密钥的常规落脚点，不要进仓库。
# 注意 .env.production 是**有意跟踪**的（构建期公开配置），别把它一并忽略掉。
.env
